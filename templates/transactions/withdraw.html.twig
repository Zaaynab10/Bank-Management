{% extends 'base.html.twig' %}

{% block stylesheets %}
    <script src="https://cdn.tailwindcss.com"></script>
{% endblock %}

{% block title %}Retrait de fonds{% endblock %}

{% block body %}
    <div class="max-w-lg mx-auto bg-white p-8 mt-10 rounded-lg shadow-md">
        <h1 class="text-2xl font-semibold text-center text-gray-700 mb-6">Retirer des fonds</h1>

        {{ form_start(form, { 'attr': { 'class': 'space-y-6' } }) }}

        <div>
            {{ form_label(form.bankAccount, 'Compte bancaire', { 'label_attr': { 'class': 'block text-gray-600 font-medium mb-2' } }) }}
            {{ form_widget(form.bankAccount, { 'attr': { 'class': 'w-full p-3 border rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none' } }) }}
        </div>

        <div>
            {{ form_label(form.amount, 'Montant', { 'label_attr': { 'class': 'block text-gray-600 font-medium mb-2' } }) }}
            {{ form_widget(form.amount, { 'attr': { 'class': 'w-full p-3 border rounded-md focus:ring-2 focus:ring-red-500 focus:outline-none', 'min': '1' } }) }}
        </div>

        <button type="submit" class="w-full bg-red-600 hover:bg-red-700 text-white font-medium py-3 rounded-md transition">
            Retirer
        </button>

        {{ form_end(form) }}
    </div>
{% endblock %}
